import { RoutePlaceholder } from "@/components/layout/route-placeholder"

export default function JobsPage() {
  return (
    <RoutePlaceholder
      eyebrow="Pipeline"
      title="Jobs"
      description="This route is ready for the jobs list, filters, and stage-based pipeline."
    />
  )
}
